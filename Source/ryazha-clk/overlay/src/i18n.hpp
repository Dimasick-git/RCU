#pragma once

namespace i18n {
    // Stub implementation: i18n::t() just returns the string as-is
    // Full i18n support can be added later if needed
    inline const char* t(const char* str) {
        return str;
    }
}
